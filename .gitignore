# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Runtime
node_modules/

# Giant 3D source files at repo root (multi-GB Shapr3D exports — never on the web).
# NOTE: content/ocu3D/*.stl are real, served models and stay tracked.
/*.stl
/content/0Kares.stl

# Local design sources not needed on the web
*.toe
/Group-PDF-Export 22.pdf

# Deploy / diagnostic cruft — never commit these again
*.zip
diag*.php
github_deploy.php
update.zip
index_backup.html
Backup/
testdir/

# 3D model SOURCES: keep locally, ship the Draco .glb instead (scripts/stl_to_glb.py)
content/**/*.stl

# Python venv per a la conversió STL→glb (local, no es publica)
.venv/
